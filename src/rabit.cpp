/*
 * rabit.cpp
 *
 *  Created on: Apr 10, 2014
 *      Author: xiao
 */

//keep alive.
/*	rtvl = setsockopt(sock_fd, SOL_SOCKET, SO_KEEPALIVE, (char*)&keepAlive, sizeof(keepAlive));
	if(rtvl < 0){
		printf("setsockopt SO_KEEPALIVE failed.");
	}
	rtvl = setsockopt(sock_fd, SOL_TCP, TCP_KEEPIDLE, (char*)&timezero, sizeof(timezero));
	if(rtvl < 0){
		printf("setsockopt TCP_KEEPIDLE failed.");
	}
	rtvl = setsockopt(sock_fd, SOL_TCP, TCP_KEEPINTVL, (char*)&timeout, sizeof(timeout));
	if(rtvl < 0){
		printf("setsockopt TCP_KEEPINTVL failed.");
	}
	rtvl = setsockopt(sock_fd, SOL_TCP, TCP_KEEPCNT, (char*)&times_snd, sizeof(times_snd));
	if(rtvl < 0){
		printf("setsockopt TCP_KEEPCNT failed.");
	}*/
